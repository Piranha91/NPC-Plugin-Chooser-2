<reactiveui:ReactiveWindow x:Class="NPC_Plugin_Chooser_2.Views.ResourcePluginSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NPC_Plugin_Chooser_2.Views"
        xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
        xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:reactiveui="http://reactiveui.net"
        mc:Ignorable="d"
        x:TypeArguments="vm:VM_ResourcePluginSelector"
        Style="{DynamicResource ThemedWindowStyle}"
        Title="Set Resource-Only Plugins" Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10">
            Select the plugins that should be treated as resource-only (do not provide NPCs themselves).
        </TextBlock>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" BorderBrush="Gray" BorderThickness="1">
            <ItemsControl ItemsSource="{Binding SelectablePlugins}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:VM_SelectableMod}">
                        <CheckBox IsChecked="{Binding IsSelected}" 
                                  Content="{Binding DisplayText}" 
                                  Margin="5,3"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" IsDefault="True"
                    Command="{Binding OKCommand}"/>
            <Button Content="Cancel" Width="80" Margin="5,0,0,0" IsCancel="True"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</reactiveui:ReactiveWindow>